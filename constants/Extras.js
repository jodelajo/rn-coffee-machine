class CoffeeType {
    constructor(
        id,
        extras,
        name,
        sizes
    ) {
        this._id = id,
        this.extras = extras,
        this.name = name,
        this.sizes = sizes
    }
}

export default CoffeeType